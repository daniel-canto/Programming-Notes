#backend/java/tecnicas_java/compilando_e_executando_um_codigo_java 

Para compilar um código Java, podemos abrir o terminal do IntelliJ e digitar o seguinte comando:
`javac Main.java`, assim conseguimos compilar a classe Main e todas as classes relacionadas à ela.

Para executar o arquivo agora compilado, usamos o comando `java Main`.